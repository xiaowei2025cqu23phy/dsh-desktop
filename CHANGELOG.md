# Changelog

本项目所有值得记录的变更。

## 未发布

### 📱 PWA 离线外壳与 HTTPS 证书
- **外壳随版本更新**:Service Worker 的缓存名改用构建版本(`dsh-remote-<版本>-<commit>`,构建期注入)。版本变化即新缓存名,`install` 重新拉取外壳、`activate` 清掉旧缓存,手机端不会再长期停在旧版 `app.js` / `app.css`。
- **换网络自动重签证书**:签发时把当时的地址一并记录,启动时校验当前所有局域网地址是否仍被证书覆盖;地址变化即重新签发。避免旧证书 SAN 不匹配导致 TLS 失败、并连带让 Service Worker 注册不上,而设置里的开关仍显示已启用。
- **证书指纹可见**:设置 → 远程访问 显示证书 SHA-256 指纹与覆盖地址,手机信任前可据此核对。该查询只读,不会触发证书生成或写盘。
- **SAN 编码收紧**:压缩形式 IPv6(`fe80::1`)与非法地址不再被静默写成 `0.0.0.0`,而是精确编码或直接拒绝,不产出"能签出却永远匹配不上"的证书。
- 新增 `scripts/tls-cert-test.mjs`:离线校验证书可解析、SAN 覆盖、私钥与证书同源、自签签名可验证、有效期未溢出 UTCTime、压缩 IPv6 正确展开、非法输入被拒、缓存复用判定。已接入 `npm test` 与 CI。

### 🔐 QQ 机器人访问控制
- **门禁改到平台侧**:QQ 开放平台的 openid 识别需要企业主体,个人主体拿不到自己的 openid,原先「白名单留空 = 锁定」会让普通用户的机器人永远不可用。现在留空 = 不限制,访问控制落在**关闭机器人的「允许被添加为好友」**上——只有电脑主人能把机器人加为好友/拉进群。桌面端**首次启用 QQ 时强制确认**这一步,未确认不启动(设置页与「状态」指令都会提示重新确认)。
- 「允许的用户 openid」降为**可选**的第二层收紧:留空不限制,填了只服务名单内 openid。想拿自己的 openid:发一条消息,被拒的 openid 会显示在设置页状态里。
- 无身份一律拒绝:群内按钮回调不携带 `user_openid` 时不再猜测归属,直接引导去私聊处理。
- 老配置兼容:已启用 QQ 的旧配置视为已确认(此前启用必须自行配置白名单,属已做过的显式决策);未启用的会在下次启用时走强制确认。
- 「扫码登录」注明走的是企业主体能力,个人主体拿不到 openid 时直接手填 AppID/AppSecret。

## 🚀 v0.6.1 · 官方 harness 0.1.2 适配与稳定性 (2026-09-11)

### 官方 harness 适配
- 主实例默认运行官方 npm `@deepseek-ai/dsh`(0.1.2-rc.1):typert 斜杠协议自动协商、浏览器 token 鉴权(launch token → 持久 cookie)、按方法自适应参数壳(`_request` / `request` / 平铺)
- 模型目录走 `session/modelCatalog`、Provider 目录走 `llm/listProviders` 与 `llm/listConfigurableProviders`;自定义 Provider 写入适配 `settings/update` / `settings/mutate` / `credentials/set`
- 会话历史回放桥:官方版无 `session.history`,改由 `session/list`(取 `projections.asOfSeq`)+ `session/page` 拉取,过滤 message 级事件;手机 PWA、QQ 与屏保历史恢复
- `session/prompt` 自动补官方要求的 `requestId`;官方缺失的 `workspace.list` 由会话 cwd 合成,`host.describe` 优雅降级
- 旧版 / 自建 fork(点协议)继续兼容,新旧实例自动探测

### 稳定性
- 托管 harness 冷启动实测约 92 秒,就绪超时由 90 秒放宽到 180 秒,消除"90 秒未就绪"误报
- npx 健康探测:可用时跟随官方最新版,不可用时自动回退本地 dsh 直连;默认启动命令附加 `--no-open`
- 开发模式(未打包)使用独立 userData,不再与正式版抢单实例锁与配置;`desktop.log` 超过 2MB 自动轮转
- 屏保激活与托管进程的错误日志输出真实原因(此前 JSON 序列化显示为 `{}`)

### 界面
- 对话页壁纸遮罩跟随「设置 → 外观 → 壁纸遮罩」(修复此前固定 0.55 导致偏暗);主窗口壁纸改用 `img` 通道,支持大图与布设位置
- 设置面板显示版本号 + commit,便于识别本地跑的是哪个构建

### 数据修复
- 修复旧 fork 会话被官方引擎拒绝的问题:用官方扫描器定位「seq 重复段」并精确删行,保留全部有效事件(案例:53MB 会话 4,080,257 事件无损保留)
- 重建预设壁纸包(此前 PNG 文件损坏)

### 文档与资源
- 支持与联系区赞赏码换成 JPG(与内测群图统一格式,白底合成);原 PNG 为损坏文件,已替换并同步中英文 README 引用

## 🚀 v0.6.0 · QQ 机器人体验大升级 (2026-09-02)

### 🤖 QQ 机器人(主要更新)
- **群聊回复稳定**:自动采用"回复该群最近一条消息"的方式(不依赖群主动消息权限),并记录每群最新消息 id,不再出现 10 分钟后失联(40034005/40034105 双兼容:开通主动消息权限后可直发)
- **群聊安全**:群里只聊天、不响应任何命令(防止他人远程操控电脑),首次进群附带安全提醒
- **任务会话合并**:未指定工作区的任务自动并入每用户「默认任务会话」;`任务 新:描述` 强制另起;`任务 @工作区` / `任务 目录:路径` / 进入工作区后直接发任务均落对应工作区
- **任务队列修复**:任务成功/失败均正确落库并放行队列(不再"运行中"卡死积压),完成推送 ✅/❌ 按会话+描述去重
- **进展阶段化**:显示 🤔 思考中 / 🔧 正在调用工具(名称+参数) / 💬 正在输出 / ✅ 已完成 / ❌ 已失败,附工具统计与「📦 最近完成」产物提示
- **推送克制**:只在 进入/查询/审批/完成/失败 时推送;任务过程默认静默,`播报` 按需开启 25 秒现场进展
- **进入工作区看家底**:`进入 qqbot` 列出会话(编号+▶/⏸状态+时间+摘要),`进入 qqbot 2` 继续,`进入 qqbot 新` 新建(归属判定修正:harness 的 sessions 字段恒空,改用 sessionIds+cwd 匹配)
- **工作区/会话罗列**:名称+会话数(去路径冗余)、过滤空会话/子代理/归档、列表带真实交付文本摘要(不再显示模型推理)
- **默认对话线程**:每人一条、穿插任务/工作区不另开、退出后自动回到原对话、`新对话` 重开
- **机器人对话可见化**:QQ 私聊/群聊纯对话统一落在自动注册的「机器人对话」工作区,手机 PWA「🤖 机器人对话」置顶组同步可见
- **归档自愈**:活动对话被归档后,下次发消息自动开新对话并提示(旧内容可用 `打开 <会话id>`/`导出` 读取)
- **聊天体验**:回复去 `💬` 前缀、回合时效 5→20 分钟、工作区助手对话回合结束推送回复、autoChat 复用旧线程不再播说明书

### 📱 手机 PWA
- 缓存写入防抖(600ms)+上限 300 条,切会话/切后台立即落盘 → 长对话明显更跟手
- 历史加载 50→40 条;远程访问断线时提示在电脑端重新启用

### 📄 文档
- 新增 `docs/QQ-BOT.md`:QQ 机器人完整指南(部署/平台权限/完整指令集/会话模型/FAQ/调试)
- 新增 `docs/PWA.md`:手机遥控特色功能指南
- README 双语同步更新

## ✨ v0.5.0 · 手机 PWA 大版本 (2026-09-02)

### 📱 手机 PWA
- **新对话工作区优先**:先选择/新建工作区,再创建对话(与 harness Web 一致);新会话自动落入所选工作区分组
- **权限预设**:新会话可选「工作区可写」/「完全访问」(harness /permission)
- **消息模式**:排队 / 插入发送(打断当前等待)
- **壁纸**:手机相册上传图片设为壁纸;内置壁纸一键切换
- **文件预览**:文本/Markdown/图片/视频(mp4/webm)/音频/PDF;浏览器禁内嵌 PDF 时提供新窗口打开/下载
- **断线自动重连**:指数退避;长时间未恢复时提示在电脑端重新启用远程访问
- **修复**:媒体流 Range 请求(视频无法播放)、新建会话被权限命令卡住

### 🖥 桌面端
- **harness 命令模板**:官方最新版 / 官方指定版本 / 本机仓库(run-local.cmd)一键选择
- 回归测试脚本 scripts/test-remote.mjs(鉴权/媒体流/白名单/打包新鲜度)
- README 双语更新

## 🚀 v0.4.0 · 会话体验与交付质量

### 手机 PWA 完整会话体验
- 会话历史加载带自动重试(瞬时断连自愈),错误自动上报桌面端审计
- 会话内展示图片产物:AI 画图后调用 read_image,图片直接显示在对话里
- 工具调用折叠为一行(参数摘要,点击展开);消息正文只显示答案,推理链不占屏
- 流式输出吸底滚动:上滑查看历史时输出自动跟随暂停,回到底部恢复
- 事件流断线自动重新取票重连,审批/提问卡片跨会话可交互

### 多实例与本地开发
- 桌面端支持「实验预览实例」:独立的端口、DSH_HOME 与工作目录,顶栏一键切换
  查看本地构建的实验版 UI,与稳定实例完全隔离
- 托管 harness 使用独立工作目录(默认 ~/dsh-workspace),Agent 产物集中管理

### 模型与配置
- 模型 provider 配置自愈:缺失的默认值(流式空闲超时、最大 token、上下文窗口)
  自动补齐,baseURL 缺 /v1 自动规范化
- 配置自愈只补缺失项,尊重已有显式配置

### 构建与质量
- 新增 No Negative Echo 交付检查(scripts/check-negative-echo.mjs):
  提交信息与说明只描述最终结果,被否方案不进入交付
- QQ 对话输出为单条流式消息(官方 stream_messages 协议):
  msg_seq 整条流固定、限流自动重试、失败只补发未显示后缀
- 扫码/图片/Telegram 网络请求走 Chromium 网络栈,跟随系统代理
- 二维码过期自动刷新,网络类错误给出排查提示

## 🔐 远程访问安全收尾

- 远程网关仅接受 loopback 与 RFC1918 私网客户端,拒绝公网来源,并在设置中明确禁止内网穿透
- 启用远程访问默认 2 小时自动关闭,支持 30 分钟/2 小时/8 小时/手动策略;重新生成令牌会使旧设备失效
- 远程 RPC、命令、审批和控制动作写入本地脱敏审计,不记录令牌或请求正文
- 外部设备即使持有 Token 也必须先获得桌面端批准;桌面端可允许/拒绝/撤销设备,未批准请求返回等待桌面批准
- 新设备扫码请求时桌面端主动弹出审批弹窗并发送系统通知,点击允许/拒绝即完成;不再需要手动去设置里找
- 审批弹窗增加轮询兜底(每 10 秒),即使 IPC 事件丢失也会弹出;扫码配对为每个局域网地址生成独立二维码(手机连哪个网络扫哪个),避免多网卡时选错地址连不上;PWA 连接失败时区分「等待批准」与「网络不通」并给出排查提示
- 事件流改用一次性短时 ticket:先用已批准设备申请 ticket,EventSource 不再携带 Token,避免 Token 进入 URL/日志
- 配对二维码改用 URL fragment 传递令牌,页面读取后立即清除地址栏,避免令牌进入 HTTP 请求日志、Referer 或浏览器历史
- PWA 重启 Harness 等危险操作增加二次确认,移除预设根目录也明确提示不会删除文件

## [v0.3.0]

### 🤖 机器人通道增强(QQ / Telegram)

- **QQ 图片理解**:对话模式下直接发图片,agent 自动看图分析(自动下载转 base64,≤10MB)
- **Token 用量统计**:`用量` 指令显示今日会话数/回合数/Token(从流式 usage 事件实时累计)
- **角色扮演**:`角色 <设定>` 为对话模式叠加角色(如"你是温柔的英语老师"),与朋友提示词叠加;`角色 无` 清除;桌面端可预设默认角色

### 🖥️ 桌面端

- **反人类设计清理**:通知设置改为即改即存(与其他配置一致,删除多余保存按钮);用量倍率删除冗余按钮;设置抽屉改为四个分组切换(通用/服务/外观/维护),不再一次滚动 14 个面板;任务队列行内直接操作(重试/取消一步到位,删除两步选中流程);工作区详情新增「📂 打开文件夹」;窗口尺寸自动记忆(重启保留,防抖落盘);Esc 关闭抽屉/弹层、弹层点击背景关闭;工作台健康面板新增「🗂️ 管理工作区」跳转;概览统计卡点击平滑滚动到对应面板
- **视觉收尾**:工作台按待处理优先重排为三列;健康摘要紧凑显示;桌面键盘焦点与窄窗口宠物避让;PWA 底部安全区与停止按钮状态优化
- **路径选择器补全**:工作区记忆路径、QQ 默认工作区/目录、AI 屏保任务工作目录三处手输路径统一增加「📁 选择文件夹」;工作区详情支持直接打开任意目录(未注册时记忆/变更/活动仍可用,健康显示提示)
- **工作区变更验收**:工作区详情新增「工作区变更」——只读查看 Git 状态(未提交文件清单)与受限 diff 预览(≤64KB,截断提示);活动详情弹层自动显示该活动工作区的 Git 变更摘要,任务成果一屏验收;越权仍受工作区/预设根白名单保护
- **任务调度队列**:任务持久化入队,**串行执行**(已有任务运行中时新任务自动排队,完成后自动启动下一条);失败自动重试(指数退避 30s/60s/120s,最多 2 次),应用重启后中断任务标记失败等待重试;工作台新增「任务队列」面板(状态/尝试次数/倒计时,支持选中后取消或立即重试);「重试 <任务ID>」指令优先走队列,运行中重试也会排队
- **主窗口导航重构**:顶栏新增「工作台 / 会话」导航,日常操作(待处理中心、AI 活动中心、工作区健康、任务记录、用量、工作区记忆、审计时间线)移出设置抽屉,设置抽屉只保留真正配置项
- **活动详情**:工作台活动中心每项可点击,弹层显示完整信息(来源/工作区/会话/时间/最近事件)与该活动关联的审计记录;活动列表显示更新时间
- **UI 舒适度优化**:顶栏精简(品牌名缩短为 DSH Desktop,状态灯移入工作台概览);工作台概览升级为 4 状态卡(Harness/待处理/运行中/今日用量,状态着色);工作台改三列均衡布局(1280 宽三列、1100 降两列、760 单列),监控面板统一限高滚动;面板标题加图标,健康列表单行紧凑显示
- **工作区视图**:顶栏新增「工作区」导航页——工作区列表(健康状态/磁盘余量)点击查看详情,详情含读写权限/会话数、该工作区本地记忆编辑(注入开关)与最近活动;记忆编辑从工作台移入该视图
- **记忆自动建议**:工作区详情「从项目文件生成草稿」——扫描 README 生成项目简介草稿、package.json 提取 build/test/dev 等常用命令与技术栈,用户确认后才保存(不自动写入长期记忆)
- **工作台首页**:概览统计(待处理数、运行中活动、今日用量) + 双栏面板布局;工作区健康检查桌面端可用
- **AI 活动中心**:统一展示 QQ、Telegram、PWA 等入口创建的任务状态、来源、工作区和最近事件;记录保存在本机,离线仍可查看
- **工作区本地记忆**:按绝对路径维护项目简介、约定、常用命令和笔记;默认不注入模型,用户明确启用后才加入匹配目录的任务上下文
- **可信审计时间线**:记录任务状态、审批、提问和记忆使用摘要,支持查看、导出和清空;不保存模型请求正文
- **通知分级与勿扰**:分别控制审批、提问、任务成功和失败通知,支持勿扰时段与紧急通知绕过
- **桌面宠物**:右下角小鲸鱼动画(工作忙碌时游得更快、泡泡更多),可拖动,双击隐藏/显示,重启记忆
- **预设工作区根目录一键选择**:设置 → 远程访问,「📁 用文件资源管理器选择文件夹」多选追加(不用手敲路径)

### 📱 手机 PWA

- **PWA 类型化**:remote/app.js(≈1800 行)迁移为 TypeScript 源(src/remote/app.ts),esbuild 打包为单文件 dist/remote/app.js(页面引用不变);新增 tsconfig.remote.json 类型检查(宽松起步,逐步收紧);构建流程集成 build-remote 步骤
- **任务队列查看**:设置面板新增「任务队列(串行执行)」,手机端可查看排队/运行/失败(含自动重试倒计时)状态
- **文件夹浏览与文件预览**:侧边栏每个工作区带「📂」按钮,浏览目录树、预览文本文件(≤64KB;仅限工作区/预设根内,越权 403)
- **文件预览增强**:PWA 文件预览支持 offset/length 分片读取(超大文本可「加载更多」继续),图片(png/jpg/jpeg/webp/gif/bmp ≤2MB)直接以 data URL 预览;白名单权限不变
- **PWA 端多种设置**:设置面板新增「预设工作区根目录」管理——查看/移除,「浏览文件夹添加」把任意可浏览目录设为预设根(手机端也能配置,不只在电脑上)
- **预设模型支持**:任务弹层模型下拉置顶「⭐ 预设模型」(桌面端设置的默认模型,默认选中);新建会话本就用预设模型
- **用量与费用(三端)**:QQ/Telegram `用量` 指令增强——按模型分组统计、💰 费用估算;PWA 设置面板新增「用量与费用」区块;桌面端可设**费用倍率**(1 = 官方价,自定义中转站按实际调整)
- **会话内切换模型**:PWA 聊天顶栏「模型」按钮,当前会话随时切换任意厂商/模型
- **桌面宠物换新装**:Gemini 生成的 DeepSeek 鲸鱼图标(SVG,深蓝渐变 + 电路 AI 图案),保留摆动/冒泡/拖动

## [v0.2.0] - 2026-08-17

### 🚀 机器人通道(QQ / Telegram)

- **审批/提问主动推送**:agent 需要审批或提问时,主动通知发起者(QQ 交互后 48h 窗口内、Telegram 即时);QQ 审批通知带「允许/拒绝」内联按钮,点一下即应答
- **扫码登录**:设置面板一键扫码,自动获取 QQ 机器人 AppID/AppSecret(参考官方 onboard 协议,凭据 AES-256-GCM 本地解密)
- **主动汇报**:任务完成/失败时主动推送(可开关);失败带错误信息
- **新指令**:`允许` / `拒绝`(审批应答)、`选 N`(选择题:多选、自定义、多题批次 `#2 选 1`)
- **默认对话模式**:非指令消息直接进入纯对话(不绑定工作区),无需指令前缀

### 📱 手机 PWA

- **审批/提问卡片**:会话中一键允许/拒绝、作答选择题(单选/多选/补充说明)
- **壁纸选择**:设置面板壁纸网格,内置包 + 本地包 + 自定义壁纸 + 默认壁纸,点击即切换
- **插入对话**:运行中「↪ 插入」按钮(`mode: steer`),打断当前等待插入新指令
- **归档会话 / 删除工作区**(仅移除注册,不删文件)
- **预设工作区**:远程端只能在电脑端配置的预设根目录下新建文件夹工作区(防越权)
- **外观定稿**:透明画框风格,壁纸清晰透出(去毛玻璃、浅色描边、白字阴影)

### 🎨 壁纸体系

- 壁纸包收纳自定义壁纸(换过的随时换回);SHA-256 哈希去重,不再产生重复副本
- 手机端列表含全部壁纸来源,当前壁纸正确高亮

### 🛡️ 远程访问安全

- 默认按可信局域网边界处理远程 PWA:仅接受 loopback 或 RFC1918 私网客户端,公网地址直接拒绝
- 启用远程访问默认 2 小时自动关闭,设置中可选择 30 分钟/2 小时/8 小时或手动模式;重新生成令牌会使旧设备失效
- 设置面板明确禁止 frp/ngrok/Cloudflare Tunnel/端口转发等内网穿透;Harness 重启等危险远程动作需要二次确认
- 远程 RPC/命令/审批/动作记录为本地脱敏审计摘要,不记录令牌或请求正文

### ⚙️ 其他

- **SQLite 持久化**:活动记录、审计时间线与任务队列从 config.json 迁移到本地 SQLite(userData/local.db,零新依赖,Electron 内置 node:sqlite);事务式写入、索引查询,首次启动自动从旧 JSON 一次性迁移;设置项仍保留在 config.json
- 启动后自动检查更新(可开关)+ 托盘更新提示
- 预设工作区根目录配置(设置 → 远程访问)
- README:功能亮点、支持与联系(内测群二维码/赞赏码)、致谢参考项目
- 安全:移除录制残留文件;发布包不含任何本地配置/壁纸/令牌

### ✅ 验证

- 命令解析测试、审批流集成测试、真实审批端到端测试(SSE 帧 → 应答 → accepted,含 PWA 卡片与按钮闭环)
- 网关上锁:越权工作区路径 403、非法文件夹名 400、未授权 401
