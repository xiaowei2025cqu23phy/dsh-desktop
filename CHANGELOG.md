# Changelog

本项目所有值得记录的变更。

## [v0.3.0] - 未发布

### 🤖 机器人通道增强(QQ / Telegram)

- **QQ 图片理解**:对话模式下直接发图片,agent 自动看图分析(自动下载转 base64,≤10MB)
- **Token 用量统计**:`用量` 指令显示今日会话数/回合数/Token(从流式 usage 事件实时累计)
- **角色扮演**:`角色 <设定>` 为对话模式叠加角色(如"你是温柔的英语老师"),与朋友提示词叠加;`角色 无` 清除;桌面端可预设默认角色

### 🖥️ 桌面端

- **反人类设计清理**:通知设置改为即改即存(与其他配置一致,删除多余保存按钮);用量倍率删除冗余按钮;设置抽屉按「⚙️ 通用设置/🔌 服务与通道/🎨 外观与扩展」分组;任务队列行内直接操作(重试/取消一步到位,删除两步选中流程);工作区详情新增「📂 打开文件夹」;窗口尺寸自动记忆(重启保留,防抖落盘);Esc 关闭抽屉/弹层、弹层点击背景关闭;工作台健康面板新增「🗂️ 管理工作区」跳转;概览统计卡点击平滑滚动到对应面板
- **路径选择器补全**:工作区记忆路径、QQ 默认工作区/目录、AI 屏保任务工作目录三处手输路径统一增加「📁 选择文件夹」;工作区详情支持直接打开任意目录(未注册时记忆/变更/活动仍可用,健康显示提示)
- **工作区变更验收**:工作区详情新增「工作区变更」——只读查看 Git 状态(未提交文件清单)与受限 diff 预览(≤64KB,截断提示);活动详情弹层自动显示该活动工作区的 Git 变更摘要,任务成果一屏验收;越权仍受工作区/预设根白名单保护
- **任务调度队列**:任务持久化入队,**串行执行**(已有任务运行中时新任务自动排队,完成后自动启动下一条);失败自动重试(指数退避 30s/60s/120s,最多 2 次),应用重启后中断任务标记失败等待重试;工作台新增「任务队列」面板(状态/尝试次数/倒计时,支持选中后取消或立即重试);「重试 <任务ID>」指令优先走队列,运行中重试也会排队
- **主窗口导航重构**:顶栏新增「工作台 / 会话」导航,日常操作(待处理中心、AI 活动中心、工作区健康、任务记录、用量、工作区记忆、审计时间线)移出设置抽屉,设置抽屉只保留真正配置项
- **活动详情**:工作台活动中心每项可点击,弹层显示完整信息(来源/工作区/会话/时间/最近事件)与该活动关联的审计记录;活动列表显示更新时间
- **UI 舒适度优化**:顶栏精简(品牌名缩短为 DSH Desktop,状态灯移入工作台概览);工作台概览升级为 4 状态卡(Harness/待处理/运行中/今日用量,状态着色);工作台改三列均衡布局(1280 宽三列、1100 降两列、760 单列),监控面板统一限高滚动;面板标题加图标,健康列表单行紧凑显示
- **工作区视图**:顶栏新增「工作区」导航页——工作区列表(健康状态/磁盘余量)点击查看详情,详情含读写权限/会话数、该工作区本地记忆编辑(注入开关)与最近活动;记忆编辑从工作台移入该视图
- **记忆自动建议**:工作区详情「从项目文件生成草稿」——扫描 README 生成项目简介草稿、package.json 提取 build/test/dev 等常用命令与技术栈,用户确认后才保存(不自动写入长期记忆)
- **工作台首页**:概览统计(待处理数、运行中活动、今日用量) + 双栏面板布局;工作区健康检查桌面端可用
- **AI 活动中心**:统一展示 QQ、Telegram、PWA 等入口创建的任务状态、来源、工作区和最近事件;记录保存在本机,离线仍可查看
- **工作区本地记忆**:按绝对路径维护项目简介、约定、常用命令和笔记;默认不注入模型,用户明确启用后才加入匹配目录的任务上下文
- **可信审计时间线**:记录任务状态、审批、提问和记忆使用摘要,支持查看、导出和清空;不保存模型请求正文
- **通知分级与勿扰**:分别控制审批、提问、任务成功和失败通知,支持勿扰时段与紧急通知绕过
- **桌面宠物**:右下角小鲸鱼动画(工作忙碌时游得更快、泡泡更多),可拖动,双击隐藏/显示,重启记忆
- **预设工作区根目录一键选择**:设置 → 远程访问,「📁 用文件资源管理器选择文件夹」多选追加(不用手敲路径)

### 📱 手机 PWA

- **PWA 类型化**:remote/app.js(≈1800 行)迁移为 TypeScript 源(src/remote/app.ts),esbuild 打包为单文件 dist/remote/app.js(页面引用不变);新增 tsconfig.remote.json 类型检查(宽松起步,逐步收紧);构建流程集成 build-remote 步骤
- **任务队列查看**:设置面板新增「任务队列(串行执行)」,手机端可查看排队/运行/失败(含自动重试倒计时)状态
- **文件夹浏览与文件预览**:侧边栏每个工作区带「📂」按钮,浏览目录树、预览文本文件(≤64KB;仅限工作区/预设根内,越权 403)
- **文件预览增强**:PWA 文件预览支持 offset/length 分片读取(超大文本可「加载更多」继续),图片(png/jpg/jpeg/webp/gif/bmp ≤2MB)直接以 data URL 预览;白名单权限不变
- **PWA 端多种设置**:设置面板新增「预设工作区根目录」管理——查看/移除,「浏览文件夹添加」把任意可浏览目录设为预设根(手机端也能配置,不只在电脑上)
- **预设模型支持**:任务弹层模型下拉置顶「⭐ 预设模型」(桌面端设置的默认模型,默认选中);新建会话本就用预设模型
- **用量与费用(三端)**:QQ/Telegram `用量` 指令增强——按模型分组统计、💰 费用估算;PWA 设置面板新增「用量与费用」区块;桌面端可设**费用倍率**(1 = 官方价,自定义中转站按实际调整)
- **会话内切换模型**:PWA 聊天顶栏「模型」按钮,当前会话随时切换任意厂商/模型
- **桌面宠物换新装**:Gemini 生成的 DeepSeek 鲸鱼图标(SVG,深蓝渐变 + 电路 AI 图案),保留摆动/冒泡/拖动

## [v0.2.0] - 2026-08-17

### 🚀 机器人通道(QQ / Telegram)

- **审批/提问主动推送**:agent 需要审批或提问时,主动通知发起者(QQ 交互后 48h 窗口内、Telegram 即时);QQ 审批通知带「允许/拒绝」内联按钮,点一下即应答
- **扫码登录**:设置面板一键扫码,自动获取 QQ 机器人 AppID/AppSecret(参考官方 onboard 协议,凭据 AES-256-GCM 本地解密)
- **主动汇报**:任务完成/失败时主动推送(可开关);失败带错误信息
- **新指令**:`允许` / `拒绝`(审批应答)、`选 N`(选择题:多选、自定义、多题批次 `#2 选 1`)
- **默认对话模式**:非指令消息直接进入纯对话(不绑定工作区),无需指令前缀

### 📱 手机 PWA

- **审批/提问卡片**:会话中一键允许/拒绝、作答选择题(单选/多选/补充说明)
- **壁纸选择**:设置面板壁纸网格,内置包 + 本地包 + 自定义壁纸 + 默认壁纸,点击即切换
- **插入对话**:运行中「↪ 插入」按钮(`mode: steer`),打断当前等待插入新指令
- **归档会话 / 删除工作区**(仅移除注册,不删文件)
- **预设工作区**:远程端只能在电脑端配置的预设根目录下新建文件夹工作区(防越权)
- **外观定稿**:透明画框风格,壁纸清晰透出(去毛玻璃、浅色描边、白字阴影)

### 🎨 壁纸体系

- 壁纸包收纳自定义壁纸(换过的随时换回);SHA-256 哈希去重,不再产生重复副本
- 手机端列表含全部壁纸来源,当前壁纸正确高亮

### ⚙️ 其他

- **SQLite 持久化**:活动记录、审计时间线与任务队列从 config.json 迁移到本地 SQLite(userData/local.db,零新依赖,Electron 内置 node:sqlite);事务式写入、索引查询,首次启动自动从旧 JSON 一次性迁移;设置项仍保留在 config.json
- 启动后自动检查更新(可开关)+ 托盘更新提示
- 预设工作区根目录配置(设置 → 远程访问)
- README:功能亮点、支持与联系(内测群二维码/赞赏码)、致谢参考项目
- 安全:移除录制残留文件;发布包不含任何本地配置/壁纸/令牌

### ✅ 验证

- 命令解析测试、审批流集成测试、真实审批端到端测试(SSE 帧 → 应答 → accepted,含 PWA 卡片与按钮闭环)
- 网关上锁:越权工作区路径 403、非法文件夹名 400、未授权 401
